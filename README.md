# Docker container with GoQt MXE environment

Avaialable on dockerhub as ```yshurik/mxe-goqt:windows_64_shared```

## Building

```
docker build -f Dockerfile.windows_64_shared -t mydockerhubuser/mxe-goqt:windows_64_shared .
```
